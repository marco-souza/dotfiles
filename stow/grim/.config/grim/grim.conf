# Grim configuration
# Default output format and behavior for screenshots

# Output format (supports PNG, JPEG, WebP, etc.)
# Default: PNG
output_format=png

# Quality for lossy formats (0-100)
quality=95

# Capture cursor in screenshots
capture_cursor=1

# Freeze the cursor position during selection
freeze_cursor=1
